<UserControl x:Class="CyberThrust.IRIS.App.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource Brush.Bg0}">
    <Grid Margin="20">
        <Border Style="{StaticResource Card}">
            <StackPanel>
                <TextBlock Text="Configurações" Style="{StaticResource H1}"/>
                <TextBlock Text="Edite appsettings.local.json para preencher tenant Entra e credenciais Falcon — pasta:" Style="{StaticResource Dim}" Margin="0,12,0,4"/>
                <TextBlock x:Name="ConfigPath" Style="{StaticResource Body}" Foreground="{DynamicResource Brush.Accent}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
