<UserControl x:Class="CyberThrust.IRIS.App.Views.IncidentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource Brush.Bg0}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolVis"/>
    </UserControl.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Cabeçalho -->
        <DockPanel Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="Alertas Unificados" Style="{StaticResource H1}"/>
                <TextBlock Text="Falcon Alerts API v2 · EDR · Identity Protection · NG-SIEM · Mobile · Cloud · OverWatch · XDR" Style="{StaticResource Dim}" Margin="0,4,0,0"/>
            </StackPanel>
            <Button DockPanel.Dock="Right" Content="Atualizar (F5)" Style="{StaticResource BtnPrimary}" Command="{Binding LoadCommand}" VerticalAlignment="Center"/>
        </DockPanel>

        <!-- Banner se não configurado -->
        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,16" Background="#22FFD740" BorderBrush="#88FFD740"
                Visibility="{Binding ShowConfigBanner, Converter={StaticResource BoolVis}}">
            <DockPanel>
                <StackPanel>
                    <TextBlock Text="⚙️ Falcon não configurado" FontWeight="SemiBold" Foreground="{DynamicResource Brush.Warn}" FontSize="14"/>
                    <TextBlock Text="Configure as credenciais Falcon em Configurações (Ctrl+8) para puxar os alertas." Style="{StaticResource Dim}" Margin="0,4,0,0"/>
                </StackPanel>
                <Button DockPanel.Dock="Right" Content="Abrir Configurações" Style="{StaticResource BtnPrimary}" Command="{Binding GoToSettingsCommand}" VerticalAlignment="Center"/>
            </DockPanel>
        </Border>

        <!-- Filtros -->
        <Border Grid.Row="2" Style="{StaticResource Card}" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Margin="0,0,8,0">
                    <TextBlock Text="PRODUTO" Style="{StaticResource Dim}" FontSize="10"/>
                    <ComboBox ItemsSource="{Binding ProductOptions}" SelectedItem="{Binding SelectedProduct}" Margin="0,4,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="4,0">
                    <TextBlock Text="SEVERIDADE MÍNIMA" Style="{StaticResource Dim}" FontSize="10"/>
                    <ComboBox ItemsSource="{Binding SeverityOptions}" SelectedItem="{Binding SelectedSeverity}" Margin="0,4,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="2" Margin="4,0">
                    <TextBlock Text="STATUS" Style="{StaticResource Dim}" FontSize="10"/>
                    <ComboBox ItemsSource="{Binding StatusOptions}" SelectedItem="{Binding SelectedStatus}" Margin="0,4,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="3" Margin="8,0,0,0">
                    <TextBlock Text="PERÍODO" Style="{StaticResource Dim}" FontSize="10"/>
                    <ComboBox ItemsSource="{Binding PeriodOptions}" SelectedItem="{Binding SelectedPeriod}" Margin="0,4,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- KPIs -->
        <UniformGrid Grid.Row="3" Rows="1" Columns="5" Margin="0,0,0,12">
            <Border Style="{StaticResource Card}" Margin="0,0,4,0" Padding="12">
                <StackPanel>
                    <TextBlock Text="TOTAL" Style="{StaticResource Dim}" FontSize="10"/>
                    <TextBlock Text="{Binding TotalCount}" FontSize="32" FontWeight="Bold" Foreground="{DynamicResource Brush.Accent}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource Card}" Margin="2,0" Padding="12">
                <StackPanel>
                    <TextBlock Text="CRÍTICAS" Style="{StaticResource Dim}" FontSize="10"/>
                    <TextBlock Text="{Binding CriticalCount}" FontSize="32" FontWeight="Bold" Foreground="{DynamicResource Brush.Critical}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource Card}" Margin="2,0" Padding="12">
                <StackPanel>
                    <TextBlock Text="ALTAS" Style="{StaticResource Dim}" FontSize="10"/>
                    <TextBlock Text="{Binding HighCount}" FontSize="32" FontWeight="Bold" Foreground="{DynamicResource Brush.Danger}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource Card}" Margin="2,0" Padding="12">
                <StackPanel>
                    <TextBlock Text="NOVOS" Style="{StaticResource Dim}" FontSize="10"/>
                    <TextBlock Text="{Binding NewCount}" FontSize="32" FontWeight="Bold" Foreground="{DynamicResource Brush.Warn}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource Card}" Margin="4,0,0,0" Padding="12">
                <StackPanel>
                    <TextBlock Text="EM PROGRESSO" Style="{StaticResource Dim}" FontSize="10"/>
                    <TextBlock Text="{Binding InProgressCount}" FontSize="32" FontWeight="Bold" Foreground="{DynamicResource Brush.Accent2}"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Lista de Alertas -->
        <Border Grid.Row="4" Style="{StaticResource Card}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <DockPanel Margin="0,0,0,12">
                    <TextBlock Text="Alertas (mais recentes)" Style="{StaticResource H2}"/>
                    <TextBlock DockPanel.Dock="Right" Text="{Binding StatusLine}" Style="{StaticResource Dim}" VerticalAlignment="Center" TextWrapping="Wrap"/>
                </DockPanel>

                <TextBlock Grid.Row="1" Text="{Binding LastError}" Foreground="{DynamicResource Brush.Danger}" TextWrapping="Wrap" Margin="0,0,0,8"
                           Visibility="{Binding HasError, Converter={StaticResource BoolVis}}"/>

                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Alerts}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{DynamicResource Brush.Bg3}" BorderBrush="{DynamicResource Brush.Stroke}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,6">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="90"/>
                                            <ColumnDefinition Width="100"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="80"/>
                                            <ColumnDefinition Width="60"/>
                                        </Grid.ColumnDefinitions>
                                        <!-- Sev chip -->
                                        <Border Background="{Binding SeverityBrush}" CornerRadius="4" Padding="6,2" HorizontalAlignment="Left" VerticalAlignment="Top">
                                            <TextBlock Text="{Binding Severity}" Foreground="Black" FontWeight="Bold" FontSize="10"/>
                                        </Border>
                                        <!-- Product chip -->
                                        <Border Grid.Column="1" BorderBrush="{Binding ProductBrush}" Background="Transparent" BorderThickness="1" CornerRadius="10" Padding="6,2" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="6,0,0,0">
                                            <TextBlock Text="{Binding ProductLabel}" Foreground="{Binding ProductBrush}" FontSize="10" FontWeight="Bold"/>
                                        </Border>
                                        <!-- Centro -->
                                        <StackPanel Grid.Column="2" Margin="12,0">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding Hostname}" FontWeight="SemiBold" Foreground="{DynamicResource Brush.Text}" FontSize="13"/>
                                                <TextBlock Text="{Binding Identity}" Foreground="{DynamicResource Brush.Accent2}" FontSize="12" Margin="8,0,0,0"/>
                                            </StackPanel>
                                            <TextBlock Text="{Binding Name}" Style="{StaticResource Body}" TextWrapping="Wrap" Margin="0,2,0,0"/>
                                            <TextBlock Text="{Binding MitreLine}" Foreground="{DynamicResource Brush.Accent}" FontSize="11" Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <!-- Status -->
                                        <TextBlock Grid.Column="3" Text="{Binding StatusLabel}" Foreground="{DynamicResource Brush.Warn}" FontSize="10" FontWeight="Bold" VerticalAlignment="Center"/>
                                        <!-- Idade -->
                                        <TextBlock Grid.Column="4" Text="{Binding AgeLabel}" Foreground="{DynamicResource Brush.TextDim}" FontSize="11" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>
