<UserControl x:Class="CyberThrust.IRIS.App.Views.ReputationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource Brush.Bg0}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolVis"/>
    </UserControl.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Cabeçalho -->
            <StackPanel Margin="0,0,0,16">
                <TextBlock Text="Reputação de Artefato" Style="{StaticResource H1}"/>
                <TextBlock Text="Consulta VirusTotal · MalwareBazaar · URLhaus · ThreatFox · API direta · zero persistência local." Style="{StaticResource Dim}" Margin="0,4,0,0"/>
            </StackPanel>

            <!-- Banner se VT key não configurada -->
            <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,16" Background="#22FFD740" BorderBrush="#88FFD740"
                    Visibility="{Binding HasVirusTotalKey, Converter={StaticResource BoolVis}, ConverterParameter=Inverted}">
                <StackPanel>
                    <TextBlock Text="⚙️  VirusTotal API key não configurada" FontWeight="SemiBold" Foreground="{DynamicResource Brush.Warn}" FontSize="14"/>
                    <TextBlock Text="Sem VT, só MalwareBazaar/URLhaus/ThreatFox são consultados. Gere uma key gratuita em virustotal.com/gui/join-us e configure em Configurações (Ctrl+8)." Style="{StaticResource Dim}" TextWrapping="Wrap" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <!-- Input -->
            <Border Grid.Row="2" Style="{StaticResource Card}" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="Artefato a analisar" Style="{StaticResource H2}"/>
                    <Grid Margin="0,12,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="180"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <ComboBox Grid.Column="0" ItemsSource="{Binding KindOptions}" SelectedItem="{Binding SelectedKind}" Background="{DynamicResource Brush.Bg3}" Foreground="{DynamicResource Brush.Text}" BorderBrush="{DynamicResource Brush.Stroke}" Padding="8" Margin="0,0,8,0"/>
                        <TextBox Grid.Column="1" Text="{Binding ArtifactInput, UpdateSourceTrigger=PropertyChanged}" Background="{DynamicResource Brush.Bg3}" Foreground="{DynamicResource Brush.Text}" BorderBrush="{DynamicResource Brush.Stroke}" Padding="8" CaretBrush="{DynamicResource Brush.Accent}" FontFamily="Consolas"/>
                        <Button Grid.Column="2" Content="Analisar" Style="{StaticResource BtnPrimary}" Command="{Binding AnalyzeCommand}" Margin="8,0,0,0"/>
                    </Grid>
                    <TextBlock Text="Exemplo: hash SHA-256, URL completa, domínio (example.com), IP (1.2.3.4)" Style="{StaticResource Dim}" Margin="0,8,0,0"/>
                    <TextBlock Text="{Binding BusyMessage}" Style="{StaticResource Dim}" Margin="0,8,0,0" Visibility="{Binding IsBusy, Converter={StaticResource BoolVis}}"/>
                </StackPanel>
            </Border>

            <!-- Veredict + score -->
            <Border Grid.Row="3" Style="{StaticResource Card}" Margin="0,0,0,16" Visibility="{Binding HasReport, Converter={StaticResource BoolVis}}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Coluna veredict -->
                    <StackPanel>
                        <TextBlock Text="VEREDICT" Style="{StaticResource Dim}"/>
                        <Border Background="{Binding VerdictBrush}" CornerRadius="6" Padding="14,8" HorizontalAlignment="Left" Margin="0,4,0,0">
                            <TextBlock Text="{Binding VerdictLabel}" FontWeight="Bold" FontSize="22" Foreground="Black"/>
                        </Border>
                        <TextBlock Text="{Binding DetectionRatioText}" Style="{StaticResource Body}" Margin="0,12,0,0" TextWrapping="Wrap"/>
                        <ProgressBar Value="{Binding DetectionRatioPercent}" Maximum="100" Height="6" Background="{DynamicResource Brush.Bg2}" Foreground="{Binding VerdictBrush}" Margin="0,8,0,0"/>
                    </StackPanel>

                    <!-- Coluna detalhes -->
                    <StackPanel Grid.Column="1" Margin="32,0,0,0">
                        <DockPanel Margin="0,0,0,6">
                            <TextBlock Text="ARTEFATO" Style="{StaticResource Dim}"/>
                        </DockPanel>
                        <TextBlock Text="{Binding Report.Artifact}" FontFamily="Consolas" FontSize="13" Foreground="{DynamicResource Brush.Accent}" TextWrapping="Wrap"/>

                        <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                            <StackPanel Margin="0,0,32,0">
                                <TextBlock Text="MALICIOSAS" Style="{StaticResource Dim}"/>
                                <TextBlock Text="{Binding Report.MaliciousCount}" FontSize="22" FontWeight="Bold" Foreground="{DynamicResource Brush.Critical}"/>
                            </StackPanel>
                            <StackPanel Margin="0,0,32,0">
                                <TextBlock Text="SUSPEITAS" Style="{StaticResource Dim}"/>
                                <TextBlock Text="{Binding Report.SuspiciousCount}" FontSize="22" FontWeight="Bold" Foreground="{DynamicResource Brush.Warn}"/>
                            </StackPanel>
                            <StackPanel Margin="0,0,32,0">
                                <TextBlock Text="LIMPAS" Style="{StaticResource Dim}"/>
                                <TextBlock Text="{Binding Report.HarmlessCount}" FontSize="22" FontWeight="Bold" Foreground="{DynamicResource Brush.Success}"/>
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="NÃO DETECTADAS" Style="{StaticResource Dim}"/>
                                <TextBlock Text="{Binding Report.UndetectedCount}" FontSize="22" FontWeight="Bold" Foreground="{DynamicResource Brush.TextDim}"/>
                            </StackPanel>
                        </StackPanel>

                        <ItemsControl ItemsSource="{Binding Report.ThreatLabels}" Margin="0,16,0,0">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#33FF0080" BorderBrush="{DynamicResource Brush.Critical}" BorderThickness="1" CornerRadius="10" Padding="8,3" Margin="0,2,4,2">
                                        <TextBlock Text="{Binding}" FontSize="11" Foreground="{DynamicResource Brush.Text}"/>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <ItemsControl ItemsSource="{Binding Report.Tags}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{DynamicResource Brush.Bg3}" BorderBrush="{DynamicResource Brush.Stroke}" BorderThickness="1" CornerRadius="10" Padding="8,3" Margin="0,2,4,2">
                                        <TextBlock Text="{Binding}" FontSize="11" Foreground="{DynamicResource Brush.TextDim}"/>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Fontes consultadas -->
            <Border Grid.Row="4" Style="{StaticResource Card}" Visibility="{Binding HasReport, Converter={StaticResource BoolVis}}">
                <StackPanel>
                    <TextBlock Text="Fontes consultadas" Style="{StaticResource H2}" Margin="0,0,0,12"/>
                    <ItemsControl ItemsSource="{Binding Report.Sources}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{DynamicResource Brush.Bg3}" BorderBrush="{DynamicResource Brush.Stroke}" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,0,0,8">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="200"/>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="100"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding Provider}" FontWeight="SemiBold" Foreground="{DynamicResource Brush.Text}"/>
                                        <TextBlock Grid.Column="1" Text="{Binding Verdict}" Foreground="{DynamicResource Brush.Accent}"/>
                                        <TextBlock Grid.Column="2" Text="{Binding Detail}" Style="{StaticResource Body}" TextWrapping="Wrap"/>
                                        <TextBlock Grid.Column="3" Text="{Binding Latency, StringFormat='{}{0:N0} ms'}" Style="{StaticResource Dim}" HorizontalAlignment="Right"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="{DynamicResource Brush.Danger}" TextWrapping="Wrap" Margin="0,8,0,0" Visibility="{Binding ErrorMessage, Converter={StaticResource BoolVis}}"/>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
