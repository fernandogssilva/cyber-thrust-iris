<UserControl x:Class="CyberThrust.IRIS.App.Views.RtrConsoleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource Brush.Bg0}">
    <Grid Margin="20">
        <Border Style="{StaticResource Card}">
            <StackPanel>
                <TextBlock Text="Console RTR" Style="{StaticResource H1}"/>
                <TextBlock Text="Shell RTR remoto contra 1..N hosts. Próxima iteração: terminal interativo + batch + script library." Style="{StaticResource Dim}" Margin="0,12,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
