<UserControl x:Class="CyberThrust.IRIS.App.Views.CtiView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource Brush.Bg0}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolVis"/>
    </UserControl.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Hero -->
            <StackPanel Margin="0,0,0,16">
                <TextBlock Text="Cyber Threat Intelligence" Style="{StaticResource H1}"/>
                <TextBlock Text="Investigação consolidada · AbuseIPDB · VirusTotal · Shodan · FOFA · zero persistência local" Style="{StaticResource Dim}" Margin="0,4,0,0"/>
            </StackPanel>

            <!-- Status das fontes -->
            <UniformGrid Grid.Row="1" Columns="4" Rows="1" Margin="0,0,0,16">
                <Border Style="{StaticResource Card}" Margin="0,0,6,0" Padding="12">
                    <StackPanel>
                        <TextBlock Text="ABUSEIPDB" Foreground="{DynamicResource Brush.TextDim}" FontSize="10"/>
                        <TextBlock Text="{Binding AbuseConfigured, Converter={StaticResource BoolVis}}" Visibility="Collapsed"/>
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                            <Ellipse Width="10" Height="10" VerticalAlignment="Center">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Setter Property="Fill" Value="{DynamicResource Brush.Warn}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding AbuseConfigured}" Value="True">
                                                <Setter Property="Fill" Value="{DynamicResource Brush.Success}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                            <TextBlock Text="Reputação IP" Foreground="{DynamicResource Brush.Text}" Margin="6,0,0,0"/>
                        </StackPanel>
                        <TextBlock Text="Confidence + reports" Foreground="{DynamicResource Brush.TextDim}" FontSize="11" Margin="0,2,0,0"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource Card}" Margin="3,0" Padding="12">
                    <StackPanel>
                        <TextBlock Text="VIRUSTOTAL" Foreground="{DynamicResource Brush.TextDim}" FontSize="10"/>
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                            <Ellipse Width="10" Height="10" VerticalAlignment="Center">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Setter Property="Fill" Value="{DynamicResource Brush.Warn}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding VtConfigured}" Value="True">
                                                <Setter Property="Fill" Value="{DynamicResource Brush.Success}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                            <TextBlock Text="Multi-engine" Foreground="{DynamicResource Brush.Text}" Margin="6,0,0,0"/>
                        </StackPanel>
                        <TextBlock Text="70+ AV engines · DNS" Foreground="{DynamicResource Brush.TextDim}" FontSize="11" Margin="0,2,0,0"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource Card}" Margin="3,0" Padding="12">
                    <StackPanel>
                        <TextBlock Text="SHODAN" Foreground="{DynamicResource Brush.TextDim}" FontSize="10"/>
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                            <Ellipse Width="10" Height="10" VerticalAlignment="Center">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Setter Property="Fill" Value="{DynamicResource Brush.Warn}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ShodanConfigured}" Value="True">
                                                <Setter Property="Fill" Value="{DynamicResource Brush.Success}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                            <TextBlock Text="Exposição" Foreground="{DynamicResource Brush.Text}" Margin="6,0,0,0"/>
                        </StackPanel>
                        <TextBlock Text="Portas · serviços · CVEs" Foreground="{DynamicResource Brush.TextDim}" FontSize="11" Margin="0,2,0,0"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource Card}" Margin="6,0,0,0" Padding="12">
                    <StackPanel>
                        <TextBlock Text="FOFA" Foreground="{DynamicResource Brush.TextDim}" FontSize="10"/>
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                            <Ellipse Width="10" Height="10" VerticalAlignment="Center">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Setter Property="Fill" Value="{DynamicResource Brush.Warn}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding FofaConfigured}" Value="True">
                                                <Setter Property="Fill" Value="{DynamicResource Brush.Success}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                            <TextBlock Text="Asset Discovery" Foreground="{DynamicResource Brush.Text}" Margin="6,0,0,0"/>
                        </StackPanel>
                        <TextBlock Text="Pivots · fingerprints" Foreground="{DynamicResource Brush.TextDim}" FontSize="11" Margin="0,2,0,0"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <!-- Input -->
            <Border Grid.Row="2" Style="{StaticResource Card}" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="Alvo da investigação" Style="{StaticResource H2}"/>
                    <Grid Margin="0,12,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="180"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <ComboBox Grid.Column="0" ItemsSource="{Binding KindOptions}" SelectedItem="{Binding SelectedKind}" Margin="0,0,8,0"/>
                        <TextBox Grid.Column="1" Text="{Binding Target, UpdateSourceTrigger=PropertyChanged}" FontFamily="Consolas"/>
                        <Button Grid.Column="2" Content="Investigar" Style="{StaticResource BtnPrimary}" Command="{Binding InvestigateCommand}" Margin="8,0,0,0"/>
                    </Grid>
                    <TextBlock Text="Ex: example.com (domínio) · 1.2.3.4 (IP) · http://malicious.com (URL) · SHA-256 hash" Style="{StaticResource Dim}" Margin="0,8,0,0"/>
                    <TextBlock Text="{Binding BusyMessage}" Style="{StaticResource Dim}" Margin="0,8,0,0" Visibility="{Binding IsBusy, Converter={StaticResource BoolVis}}"/>
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="{DynamicResource Brush.Danger}" Margin="0,8,0,0" Visibility="{Binding ErrorMessage, Converter={StaticResource BoolVis}}"/>
                </StackPanel>
            </Border>

            <!-- Verdict + Reputação -->
            <Border Grid.Row="3" Style="{StaticResource Card}" Margin="0,0,0,16" Visibility="{Binding HasReport, Converter={StaticResource BoolVis}}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="220"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="VEREDICT GLOBAL" Style="{StaticResource Dim}"/>
                        <Border Background="{Binding VerdictBrush}" CornerRadius="6" Padding="14,8" HorizontalAlignment="Left" Margin="0,4,0,0">
                            <TextBlock Text="{Binding VerdictLabel}" FontWeight="Bold" FontSize="22" Foreground="Black"/>
                        </Border>
                        <TextBlock Text="ALVO" Style="{StaticResource Dim}" Margin="0,16,0,0"/>
                        <TextBlock Text="{Binding Report.Target}" FontFamily="Consolas" Foreground="{DynamicResource Brush.Accent}" TextWrapping="Wrap"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="32,0,0,0" Visibility="{Binding HasReputation, Converter={StaticResource BoolVis}}">
                        <TextBlock Text="REPUTAÇÃO" Style="{StaticResource Dim}"/>
                        <UniformGrid Rows="1" Columns="4" Margin="0,8,0,0">
                            <StackPanel Margin="0,0,12,0">
                                <TextBlock Text="ABUSE SCORE" Foreground="{DynamicResource Brush.TextDim}" FontSize="10"/>
                                <TextBlock Text="{Binding Report.Reputation.AbuseConfidenceScore, StringFormat='{}{0}/100'}" FontSize="22" FontWeight="Bold" Foreground="{DynamicResource Brush.Critical}"/>
                            </StackPanel>
                            <StackPanel Margin="0,0,12,0">
                                <TextBlock Text="VT MALICIOSOS" Foreground="{DynamicResource Brush.TextDim}" FontSize="10"/>
                                <TextBlock FontSize="22" FontWeight="Bold" Foreground="{DynamicResource Brush.Danger}">
                                    <Run Text="{Binding Report.Reputation.VtMaliciousEngines}"/><Run Text=" / "/><Run Text="{Binding Report.Reputation.VtTotalEngines}"/>
                                </TextBlock>
                            </StackPanel>
                            <StackPanel Margin="0,0,12,0">
                                <TextBlock Text="REPORTS" Foreground="{DynamicResource Brush.TextDim}" FontSize="10"/>
                                <TextBlock Text="{Binding Report.Reputation.AbuseTotalReports}" FontSize="22" FontWeight="Bold" Foreground="{DynamicResource Brush.Warn}"/>
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="PAÍS · ASN" Foreground="{DynamicResource Brush.TextDim}" FontSize="10"/>
                                <TextBlock FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource Brush.Text}" TextWrapping="Wrap">
                                    <Run Text="{Binding Report.Reputation.Country}"/>
                                </TextBlock>
                                <TextBlock Text="{Binding Report.Reputation.Asn}" Foreground="{DynamicResource Brush.TextDim}" FontSize="11"/>
                            </StackPanel>
                        </UniformGrid>
                        <TextBlock Text="{Binding Report.Reputation.Isp}" Foreground="{DynamicResource Brush.TextDim}" FontSize="11" Margin="0,8,0,0"/>
                        <ItemsControl ItemsSource="{Binding Report.Reputation.Categories}" Margin="0,8,0,0">
                            <ItemsControl.ItemsPanel><ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#33FF0080" BorderBrush="{DynamicResource Brush.Critical}" BorderThickness="1" CornerRadius="10" Padding="6,2" Margin="0,2,4,2">
                                        <TextBlock Text="{Binding}" Foreground="{DynamicResource Brush.Text}" FontSize="11"/>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Exposição (Shodan) -->
            <Border Grid.Row="4" Style="{StaticResource Card}" Margin="0,0,0,16" Visibility="{Binding HasExposure, Converter={StaticResource BoolVis}}">
                <StackPanel>
                    <DockPanel>
                        <TextBlock Text="Exposição na Internet (Shodan)" Style="{StaticResource H2}"/>
                        <TextBlock DockPanel.Dock="Right" Foreground="{DynamicResource Brush.TextDim}" VerticalAlignment="Center" FontSize="11">
                            <Run Text="{Binding Report.Exposure.Country}"/><Run Text=" · "/><Run Text="{Binding Report.Exposure.City}"/><Run Text=" · ASN "/><Run Text="{Binding Report.Exposure.Asn}"/>
                        </TextBlock>
                    </DockPanel>
                    <TextBlock Text="{Binding Report.Exposure.Org}" Foreground="{DynamicResource Brush.Accent2}" Margin="0,4,0,12"/>
                    <ItemsControl ItemsSource="{Binding Report.Exposure.OpenPorts}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{DynamicResource Brush.Bg3}" BorderBrush="{DynamicResource Brush.Stroke}" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,6">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="70"/>
                                            <ColumnDefinition Width="160"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Foreground="{DynamicResource Brush.Accent}" FontWeight="Bold" FontSize="16">
                                                <Run Text="{Binding Port}"/>
                                            </TextBlock>
                                            <TextBlock Text="{Binding Transport}" Foreground="{DynamicResource Brush.TextDim}" FontSize="10"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="{Binding ServiceName}" Foreground="{DynamicResource Brush.Accent2}" FontWeight="SemiBold"/>
                                            <TextBlock FontFamily="Consolas" Foreground="{DynamicResource Brush.Text}" FontSize="11">
                                                <Run Text="{Binding Product}"/><Run Text=" "/><Run Text="{Binding Version}"/>
                                            </TextBlock>
                                        </StackPanel>
                                        <TextBlock Grid.Column="2" Text="{Binding Banner}" FontFamily="Consolas" FontSize="10" Foreground="{DynamicResource Brush.TextDim}" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <!-- Vulnerabilidades -->
            <Border Grid.Row="5" Style="{StaticResource Card}" Margin="0,0,0,16" Visibility="{Binding HasVulnerabilities, Converter={StaticResource BoolVis}}">
                <StackPanel>
                    <TextBlock Text="Vulnerabilidades Conhecidas (CVE)" Style="{StaticResource H2}" Margin="0,0,0,12"/>
                    <ItemsControl ItemsSource="{Binding Report.Vulnerabilities}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#22FF0080" BorderBrush="{DynamicResource Brush.Critical}" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,0,0,6">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="160"/>
                                            <ColumnDefinition Width="90"/>
                                            <ColumnDefinition Width="80"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding CveId}" FontFamily="Consolas" Foreground="{DynamicResource Brush.Critical}" FontWeight="Bold"/>
                                        <TextBlock Grid.Column="1" Text="{Binding Severity}" Foreground="{DynamicResource Brush.Warn}" FontWeight="SemiBold"/>
                                        <TextBlock Grid.Column="2" Text="{Binding CvssScore, StringFormat='CVSS {0:N1}'}" Foreground="{DynamicResource Brush.Accent}"/>
                                        <TextBlock Grid.Column="3" Text="{Binding Summary}" Style="{StaticResource Body}" TextWrapping="Wrap"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <!-- Pivots -->
            <Border Grid.Row="6" Style="{StaticResource Card}" Visibility="{Binding HasPivots, Converter={StaticResource BoolVis}}">
                <StackPanel>
                    <TextBlock Text="Pivots — assets relacionados" Style="{StaticResource H2}" Margin="0,0,0,12"/>
                    <ItemsControl ItemsSource="{Binding Report.Pivots}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{DynamicResource Brush.Bg3}" BorderBrush="{DynamicResource Brush.Stroke}" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,4">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="100"/>
                                            <ColumnDefinition Width="80"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Source}" Foreground="{DynamicResource Brush.Accent}" FontSize="11" FontWeight="Bold"/>
                                        <TextBlock Grid.Column="1" Text="{Binding Kind}" Foreground="{DynamicResource Brush.Accent2}" FontSize="11"/>
                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text="{Binding Indicator}" FontFamily="Consolas" Foreground="{DynamicResource Brush.Text}" FontSize="12" TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Text="{Binding Detail}" Foreground="{DynamicResource Brush.TextDim}" FontSize="10" TextTrimming="CharacterEllipsis"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
