<UserControl x:Class="CyberThrust.IRIS.App.Views.WelcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource Brush.Bg0}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolVis"/>
    </UserControl.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>  <!-- Hero -->
                <RowDefinition Height="Auto"/>  <!-- 4 capability cards -->
                <RowDefinition Height="Auto"/>  <!-- Falcon KPIs (se conectado) -->
                <RowDefinition Height="Auto"/>  <!-- Quick actions -->
                <RowDefinition Height="*"/>     <!-- Live feed -->
            </Grid.RowDefinitions>

            <!-- HERO -->
            <Border Grid.Row="0" CornerRadius="14" Padding="32" Margin="0,0,0,20" BorderThickness="1">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#FF050810" Offset="0"/>
                        <GradientStop Color="#FF0B1228" Offset="0.6"/>
                        <GradientStop Color="#FF080A18" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Border.BorderBrush>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#FF00E5FF" Offset="0"/>
                        <GradientStop Color="#FF7C4DFF" Offset="1"/>
                    </LinearGradientBrush>
                </Border.BorderBrush>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                            <Ellipse Width="12" Height="12" Fill="#FF00E5FF" VerticalAlignment="Center"/>
                            <TextBlock Text="CYBER THRUST OPERATIONS CENTER" Foreground="#FF8C9BC9" FontSize="11" VerticalAlignment="Center" Margin="10,0,0,0"/>
                        </StackPanel>
                        <TextBlock>
                            <Run Text="{Binding Greeting}" Foreground="{DynamicResource Brush.Text}" FontSize="38" FontWeight="Bold"/>
                            <Run Text="—" Foreground="{DynamicResource Brush.TextDim}" FontSize="34"/>
                            <Run Text="IRIS" Foreground="{DynamicResource Brush.Accent}" FontSize="38" FontWeight="Bold"/>
                        </TextBlock>
                        <TextBlock Text="{Binding CurrentDateTime}" Foreground="{DynamicResource Brush.TextDim}" FontSize="13" Margin="0,4,0,0"/>
                        <TextBlock Text="{Binding OperatorLine}" Foreground="{DynamicResource Brush.TextDim}" FontSize="12" Margin="0,8,0,0"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" VerticalAlignment="Top">
                        <Button Content="Login Entra ID" Style="{StaticResource BtnPrimary}" Command="{Binding GoToLoginCommand}" HorizontalAlignment="Right" Margin="0,0,0,8"/>
                        <Button Content="Atualizar (F5)" Style="{StaticResource BtnGhost}" Command="{Binding RefreshCommand}" HorizontalAlignment="Right"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- 4 STATUS CARDS -->
            <UniformGrid Grid.Row="1" Columns="4" Rows="1" Margin="0,0,0,20">
                <!-- Entra ID -->
                <Border Style="{StaticResource Card}" Margin="0,0,6,0">
                    <StackPanel>
                        <TextBlock Text="MICROSOFT ENTRA ID" Foreground="{DynamicResource Brush.TextDim}" FontSize="10"/>
                        <TextBlock Text="Autenticação do operador" Foreground="{DynamicResource Brush.TextDim}" FontSize="11" Margin="0,2,0,8"/>
                        <Border CornerRadius="4" Padding="8,2" HorizontalAlignment="Left" Background="{Binding EntraStatusBrush}">
                            <TextBlock Text="{Binding EntraStatus}" Foreground="Black" FontWeight="Bold" FontSize="11"/>
                        </Border>
                        <TextBlock Text="{Binding EntraStatusDetail}" Style="{StaticResource Body}" Margin="0,8,0,0" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <!-- CrowdStrike Falcon -->
                <Border Style="{StaticResource Card}" Margin="3,0">
                    <StackPanel>
                        <TextBlock Text="CROWDSTRIKE FALCON" Foreground="{DynamicResource Brush.TextDim}" FontSize="10"/>
                        <TextBlock Text="EDR / RTR / Hosts / Detections" Foreground="{DynamicResource Brush.TextDim}" FontSize="11" Margin="0,2,0,8"/>
                        <Border CornerRadius="4" Padding="8,2" HorizontalAlignment="Left" Background="{Binding FalconStatusBrush}">
                            <TextBlock Text="{Binding FalconStatus}" Foreground="Black" FontWeight="Bold" FontSize="11"/>
                        </Border>
                        <TextBlock Text="{Binding FalconStatusDetail}" Style="{StaticResource Body}" Margin="0,8,0,0" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <!-- Threat Intel -->
                <Border Style="{StaticResource Card}" Margin="3,0">
                    <StackPanel>
                        <TextBlock Text="THREAT INTELLIGENCE" Foreground="{DynamicResource Brush.TextDim}" FontSize="10"/>
                        <TextBlock Text="URLhaus · ThreatFox · Bazaar" Foreground="{DynamicResource Brush.TextDim}" FontSize="11" Margin="0,2,0,8"/>
                        <Border CornerRadius="4" Padding="8,2" HorizontalAlignment="Left" Background="{Binding ThreatIntelStatusBrush}">
                            <TextBlock Text="{Binding ThreatIntelStatus}" Foreground="Black" FontWeight="Bold" FontSize="11"/>
                        </Border>
                        <TextBlock Text="{Binding ThreatIntelStatusDetail}" Style="{StaticResource Body}" Margin="0,8,0,0" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <!-- Reputation -->
                <Border Style="{StaticResource Card}" Margin="6,0,0,0">
                    <StackPanel>
                        <TextBlock Text="REPUTAÇÃO DE ARTEFATO" Foreground="{DynamicResource Brush.TextDim}" FontSize="10"/>
                        <TextBlock Text="VirusTotal · análise multi-engine" Foreground="{DynamicResource Brush.TextDim}" FontSize="11" Margin="0,2,0,8"/>
                        <Border CornerRadius="4" Padding="8,2" HorizontalAlignment="Left" Background="{Binding ReputationStatusBrush}">
                            <TextBlock Text="{Binding ReputationStatus}" Foreground="Black" FontWeight="Bold" FontSize="11"/>
                        </Border>
                        <TextBlock Text="{Binding ReputationStatusDetail}" Style="{StaticResource Body}" Margin="0,8,0,0" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <!-- FALCON KPIs (só se conectado) -->
            <Border Grid.Row="2" Style="{StaticResource Card}" Margin="0,0,0,20"
                    Visibility="{Binding HasFalconKpis, Converter={StaticResource BoolVis}}">
                <StackPanel>
                    <DockPanel Margin="0,0,0,12">
                        <TextBlock Text="Visão Falcon — frota em tempo real" Style="{StaticResource H2}"/>
                        <TextBlock DockPanel.Dock="Right" Text="{Binding KpiLastUpdate, StringFormat='Última atualização: {0}'}" Style="{StaticResource Dim}" VerticalAlignment="Center"/>
                    </DockPanel>
                    <UniformGrid Rows="1" Columns="4">
                        <StackPanel Margin="0,0,16,0">
                            <TextBlock Text="HOSTS ATIVOS" Foreground="{DynamicResource Brush.TextDim}" FontSize="10"/>
                            <TextBlock Text="{Binding KpiHosts}" FontSize="34" FontWeight="Bold" Foreground="{DynamicResource Brush.Accent}"/>
                            <TextBlock Text="status='normal'" Style="{StaticResource Dim}" FontSize="10"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,16,0">
                            <TextBlock Text="DETECÇÕES (50)" Foreground="{DynamicResource Brush.TextDim}" FontSize="10"/>
                            <TextBlock Text="{Binding KpiDetections}" FontSize="34" FontWeight="Bold" Foreground="{DynamicResource Brush.Accent2}"/>
                            <TextBlock Text="mais recentes" Style="{StaticResource Dim}" FontSize="10"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,16,0">
                            <TextBlock Text="CRÍTICAS+ALTAS" Foreground="{DynamicResource Brush.TextDim}" FontSize="10"/>
                            <TextBlock Text="{Binding KpiCritical}" FontSize="34" FontWeight="Bold" Foreground="{DynamicResource Brush.Critical}"/>
                            <TextBlock Text="ação requerida" Style="{StaticResource Dim}" FontSize="10"/>
                        </StackPanel>
                        <StackPanel>
                            <TextBlock Text="FALCON CLOUD" Foreground="{DynamicResource Brush.TextDim}" FontSize="10"/>
                            <TextBlock Text="{Binding KpiCloud}" FontSize="34" FontWeight="Bold" Foreground="{DynamicResource Brush.Success}"/>
                            <TextBlock Text="região da API" Style="{StaticResource Dim}" FontSize="10"/>
                        </StackPanel>
                    </UniformGrid>
                </StackPanel>
            </Border>

            <!-- QUICK ACTIONS -->
            <Border Grid.Row="3" Style="{StaticResource Card}" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Ações rápidas" Style="{StaticResource H2}" Margin="0,0,0,12"/>
                    <UniformGrid Rows="1" Columns="4">
                        <Button Style="{StaticResource BtnGhost}" Command="{Binding GoToDashboardCommand}" Margin="0,0,6,0" Padding="0">
                            <Border BorderBrush="{DynamicResource Brush.Accent}" BorderThickness="1" Background="#1100E5FF" CornerRadius="6" Padding="14">
                                <StackPanel>
                                    <TextBlock Text="📊" FontSize="22"/>
                                    <TextBlock Text="Dashboard" FontWeight="SemiBold" Foreground="{DynamicResource Brush.Text}" Margin="0,4,0,0"/>
                                    <TextBlock Text="Detecções recentes em cards estilo Cybereason" Foreground="{DynamicResource Brush.TextDim}" FontSize="11" Margin="0,2,0,0" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Border>
                        </Button>
                        <Button Style="{StaticResource BtnGhost}" Command="{Binding GoToReputationCommand}" Margin="3,0" Padding="0">
                            <Border BorderBrush="{DynamicResource Brush.Accent2}" BorderThickness="1" Background="#117C4DFF" CornerRadius="6" Padding="14">
                                <StackPanel>
                                    <TextBlock Text="🔬" FontSize="22"/>
                                    <TextBlock Text="Reputação" FontWeight="SemiBold" Foreground="{DynamicResource Brush.Text}" Margin="0,4,0,0"/>
                                    <TextBlock Text="VirusTotal · MalwareBazaar · URLhaus · ThreatFox" Foreground="{DynamicResource Brush.TextDim}" FontSize="11" Margin="0,2,0,0" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Border>
                        </Button>
                        <Button Style="{StaticResource BtnGhost}" Command="{Binding GoToHealthCommand}" Margin="3,0" Padding="0">
                            <Border BorderBrush="{DynamicResource Brush.Success}" BorderThickness="1" Background="#1100E676" CornerRadius="6" Padding="14">
                                <StackPanel>
                                    <TextBlock Text="✓" FontSize="22"/>
                                    <TextBlock Text="Health Check" FontWeight="SemiBold" Foreground="{DynamicResource Brush.Text}" Margin="0,4,0,0"/>
                                    <TextBlock Text="12 auto-validações com código de erro IRIS-*" Foreground="{DynamicResource Brush.TextDim}" FontSize="11" Margin="0,2,0,0" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Border>
                        </Button>
                        <Button Style="{StaticResource BtnGhost}" Command="{Binding GoToSettingsCommand}" Margin="6,0,0,0" Padding="0">
                            <Border BorderBrush="{DynamicResource Brush.Warn}" BorderThickness="1" Background="#11FFD740" CornerRadius="6" Padding="14">
                                <StackPanel>
                                    <TextBlock Text="⚙️" FontSize="22"/>
                                    <TextBlock Text="Configurações" FontWeight="SemiBold" Foreground="{DynamicResource Brush.Text}" Margin="0,4,0,0"/>
                                    <TextBlock Text="Entra ID + Falcon + VirusTotal + Exfil" Foreground="{DynamicResource Brush.TextDim}" FontSize="11" Margin="0,2,0,0" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Border>
                        </Button>
                    </UniformGrid>
                </StackPanel>
            </Border>

            <!-- LIVE THREAT FEED -->
            <Border Grid.Row="4" Style="{StaticResource Card}">
                <StackPanel>
                    <DockPanel Margin="0,0,0,12">
                        <StackPanel>
                            <TextBlock Text="Live Threat Intelligence Feed" Style="{StaticResource H2}"/>
                            <TextBlock Text="{Binding FeedLastUpdate}" Style="{StaticResource Dim}" Margin="0,2,0,0"/>
                        </StackPanel>
                        <Border DockPanel.Dock="Right" Background="#22FF0080" BorderBrush="{DynamicResource Brush.Critical}" BorderThickness="1" CornerRadius="10" Padding="8,3" VerticalAlignment="Center">
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="6" Height="6" Fill="{DynamicResource Brush.Critical}" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <TextBlock Text="LIVE" Foreground="{DynamicResource Brush.Critical}" FontSize="10" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>
                    </DockPanel>

                    <Grid Visibility="{Binding IsLoadingFeed, Converter={StaticResource BoolVis}}">
                        <ProgressBar IsIndeterminate="True" Height="3" Foreground="{DynamicResource Brush.Accent}" Background="{DynamicResource Brush.Bg2}" BorderThickness="0"/>
                    </Grid>

                    <ItemsControl ItemsSource="{Binding LiveFeed}" Margin="0,4,0,0">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{DynamicResource Brush.Bg3}" BorderBrush="{DynamicResource Brush.Stroke}" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,0,0,6">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="60"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="180"/>
                                            <ColumnDefinition Width="60"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Background="#22FF0080" BorderBrush="{DynamicResource Brush.Critical}" BorderThickness="1" CornerRadius="4" Padding="6,2" HorizontalAlignment="Left">
                                            <TextBlock Text="{Binding Source}" Foreground="{DynamicResource Brush.Critical}" FontSize="10" FontWeight="Bold"/>
                                        </Border>
                                        <TextBlock Grid.Column="1" Text="{Binding Type}" Foreground="{DynamicResource Brush.Accent2}" FontSize="11" VerticalAlignment="Center" Margin="6,0,0,0"/>
                                        <TextBlock Grid.Column="2" Text="{Binding ShortIndicator}" FontFamily="Consolas" FontSize="12" Foreground="{DynamicResource Brush.Text}" VerticalAlignment="Center" Margin="6,0,0,0" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Grid.Column="3" Text="{Binding Threat}" Foreground="{DynamicResource Brush.Warn}" FontSize="11" VerticalAlignment="Center" Margin="6,0,0,0" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Grid.Column="4" Text="{Binding AgeLabel}" Foreground="{DynamicResource Brush.TextDim}" FontSize="11" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <TextBlock Text="Fonte: abuse.ch (URLhaus, ThreatFox, MalwareBazaar) · APIs públicas · sem persistência local" Style="{StaticResource Dim}" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
