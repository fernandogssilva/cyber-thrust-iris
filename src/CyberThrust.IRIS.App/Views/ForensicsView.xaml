<UserControl x:Class="CyberThrust.IRIS.App.Views.ForensicsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource Brush.Bg0}">
    <Grid Margin="20">
        <Border Style="{StaticResource Card}">
            <StackPanel>
                <TextBlock Text="Forense (disco)" Style="{StaticResource H1}"/>
                <TextBlock Text="KAPE / Velociraptor / UAC via RTR — exfil para S3/Azure Blob. Próxima iteração: wizard de coleta." Style="{StaticResource Dim}" Margin="0,12,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
